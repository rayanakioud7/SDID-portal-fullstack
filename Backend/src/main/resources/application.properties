# 1. Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/gestion_cours_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=rayan2004

# 2. JPA / Hibernate (The Engine)
# This setting AUTOMATICALLY creates tables based on your Java classes
spring.jpa.hibernate.ddl-auto=update

# 3. Formatting (Optional, helps debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

spring.web.resources.static-locations=classpath:/static/,file:uploads/

